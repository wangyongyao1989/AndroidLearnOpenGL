# AndroidLearnOpenGL 项目操作指南与目的概述

## 1. 核心操作流程
本项目通过层次化的导航结构，引导用户从 Java 业务层逐步深入到 Native 渲染层，掌握 OpenGL ES 3.0 的核心技术。

### 阶段一：学习模块选择 (主界面)
*   **操作**：在 App 首页点击“OpenGL基础”、“OpenGL 3D”、“OpenGL高级”或“着色器编程”卡片。
*   **目的**：确定当前的学习主题。项目将渲染任务按难度和功能进行分类，方便开发者循序渐地进学习。

### 阶段二：具体示例切换 (模块侧边栏)
*   **操作**：进入具体模块后，使用左侧滑动列表选择不同的技术示例（如“GL纹理”、“多光源”、“骨骼动画”）。
*   **目的**：触发底层渲染引擎的重置与重建。每次点击都会通过 JNI 通知 C++ 层加载对应的 GLSL 着色器文件、顶点缓冲对象 (VBO) 和纹理资源。

### 阶段三：实时效果交互 (渲染区域)
*   **操作**：
    *   **3D 交互**：在 3D 模型或摄像机示例中，在右侧渲染区域进行滑动或缩放。
    *   **参数切换**：在“高级”或“着色器”模块中，点击右下角的悬浮按钮（如“后期处理切换”）。
*   **目的**：
    *   通过修改观察矩阵 (View Matrix) 和投影矩阵 (Projection Matrix)，直观理解 3D 空间的透视与运动。
    *   实时观测不同着色器算法的输出（如灰度化、反相、卷积核效果），实现代码逻辑与视觉表现的即时关联。

### 阶段四：返回主导航
*   **操作**：点击页面左上角的“返回”按钮。
*   **目的**：销毁当前的 GL 视图资源，释放显存，回到主菜单准备进入下一个学习单元。

## 2. 技术操作与目的概括总结

| 操作环节 | 行为目的 | 实现原理 (底层逻辑) |
| :--- | :--- | :--- |
| **模块跳转** | 逻辑隔离与资源管理 | 利用 Fragment 事务管理不同渲染模块的生命周期 |
| **列表点击** | 渲染场景的动态重建 | JNI 调用 `native_init_opengl`，重新执行 `glCreateProgram` 及顶点属性绑定 |
| **手势操作** | 3D 空间感仿真 | 将 Java 层坐标传递至 Native `Camera3D` 类，计算并更新 MVP 矩阵 |
| **子类型切换** | 对比不同渲染算法 | 动态修改 GLSL 中的 `uniform` 变量或更新渲染管线的状态位 |
| **日志观测** | 调试与流程理解 | 通过 Android Logcat 监控着色器编译过程及 GPU 内存状态 |

---
*本项目不仅是一个演示工具，更是一套完整的 OpenGL ES 工程化实践模板。*
